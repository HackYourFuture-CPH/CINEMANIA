import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    mainGreen: '#00FFC2',
    hoverRed: '#FF4000',
  },
  typography: {
    fontFamily: "'Inter', sans-serif",
  },
});
